package com.github.cgund98.schemaregistry

/**
 * Schema registry library: event types, envelope handling, and codegen utilities.
 * Applications (publisher, consumer) depend on this module.
 */
object SchemaRegistry {
    // Placeholder for schema registry implementation.
}
