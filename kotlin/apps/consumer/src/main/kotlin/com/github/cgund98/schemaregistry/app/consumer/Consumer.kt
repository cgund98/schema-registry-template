package com.github.cgund98.schemaregistry.app.consumer

import com.github.cgund98.schemaregistry.SchemaRegistry

/**
 * Demo consumer entrypoint: consumes events using the schema registry.
 */
fun main() {
    println("Demo consumer (schema registry: $SchemaRegistry)")
}
